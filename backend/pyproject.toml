[project]
name = "backend"
version = "0.1.0"
description = "Acme Portal API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.uv]
package = false
